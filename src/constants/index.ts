// Change rows and cols according to diffivulty of game
// Don't put no of bombs as rows * cols, will go into inf recursion (game's logic won't start)
export const MAX_ROWS = 9;
export const MAX_COLS = 9;
export const NO_OF_BOMBS = 12;
